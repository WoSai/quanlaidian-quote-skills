# ⑨ 瑞幸咖啡 120 店 · 翻车原因

## 违反规则

- **规则 ①**（SKILL.md 第 124 行、180 行）：`轻餐连锁供应链版` 包含『单门店库存』，与总部 `配送中心`/`生产加工` 是两条互斥的供应链路线，service 端校验 400。
- **规则 ②**（SKILL.md 第 125 行）：勾选了 `配送中心` / `生产加工`，但门店增值模块没有 `供应链基础-门店点位` 承接收货，API 同样 400。

## 自动修正

`tests.rules.auto_correct` 触发两条修正：

1. `门店套餐: 轻餐连锁供应链版` → `轻餐连锁营销全能版`
2. `门店增值模块` 追加 `供应链基础-门店点位`

## 价格基线

售前历史测试在 beta API 上得出 ¥214,876（120 店落 100 锚点）。本 fixture 的 `配送中心数量=2, 生产加工中心数量=1` 是售前未给完整明细时的合理还原；`expected.final_price` 标了 `price_baseline_only=true`，--live 模式不强校验，只要求 API 返回 200。

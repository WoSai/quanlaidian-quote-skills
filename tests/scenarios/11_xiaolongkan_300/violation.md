# ⑪ 小龙坎 300 店 · 翻车原因

## 违反规则

- **规则 ①**（SKILL.md『卡片答案 → API 字段修正映射』+『套餐默认决策树』）：`正餐连锁营销旗舰版` 含『单门店库存』，与配送中心互斥。
- **规则 ②**（SKILL.md『卡片答案 → API 字段修正映射』）：门店增值模块未含 `供应链基础-门店点位`，无法承接配送中心收货。

## 自动修正

`tests.rules.auto_correct` 触发两条：

1. `门店套餐: 正餐连锁营销旗舰版` → `正餐连锁营销全能版`
2. `门店增值模块` 追加 `供应链基础-门店点位`

## 价格基线

售前历史测试在 beta API 上得 ¥673,701（300 店在 large-segment-v1 中观察到 effective=200，按下锚点处理）。`配送中心数量=5` 是合理还原；`price_baseline_only=true` 不强校验。

`effective=200` 而非 300 这一行为是售前实际观测，与 SKILL.md『大客户段（31–300 店）处理规则』的『按下锚点（50 / 100 / 200 / 300）』表述一致——上限 300 的输入也走 200 锚点，300 锚点目前未在 API 中独立报价档。

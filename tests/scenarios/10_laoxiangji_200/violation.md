# ⑩ 老乡鸡 200 店 · 翻车原因

## 违反规则

- **规则 ②**（SKILL.md 第 125 行）：勾了 `配送中心` 但门店增值模块未含 `供应链基础-门店点位`，门店端没法承接配送收货，API 直接 400。

注意套餐选了 `正餐连锁营销全能版`，没有踩到规则①——这是售前对决策树读了一半的典型形态：版本选对了，配套模块漏了。

## 自动修正

`tests.rules.auto_correct` 只触发一条：`门店增值模块` 追加 `供应链基础-门店点位`。套餐字段不动。

## 价格基线

售前历史测试在 beta API 上得 ¥671,926（200 店命中 200 锚点，无 effective 漂移）。本 fixture 的 `配送中心数量=5` 是合理还原；`price_baseline_only=true` 不强校验。
